@extends('layouts/layoutMaster')
@section('title', 'Customer Dashboard')
@section('content')
<div class="mb-4"><span class="text-primary fw-semibold text-uppercase small">Customer portal</span><h4 class="mb-1">{{ $customer->company_name }}</h4><p class="text-muted mb-0">Your RFQs, quotations, and complaint status.</p></div>
<div class="row g-4"><div class="col-md-4"><div class="card"><div class="card-body"><small class="text-muted">Total RFQs</small><h3>{{ $customer->rfqs->count() }}</h3></div></div></div><div class="col-md-4"><div class="card"><div class="card-body"><small class="text-muted">Open complaints</small><h3>{{ $customer->complaints->whereIn('status', ['open', 'in_progress'])->count() }}</h3></div></div></div><div class="col-md-4"><div class="card"><div class="card-body"><small class="text-muted">Contact</small><h5>{{ $customer->contact_person }}</h5><p class="mb-0">{{ $customer->email }}</p></div></div></div></div>
<div class="row g-4 mt-1"><div class="col-lg-7"><div class="card"><div class="card-header"><h5 class="mb-0">RFQ status</h5></div><div class="table-responsive"><table class="table mb-0"><thead><tr><th>RFQ</th><th>Date</th><th>Status</th></tr></thead><tbody>@forelse($customer->rfqs as $rfq)<tr><td>{{ $rfq->rfq_number }}</td><td>{{ $rfq->rfq_received_date?->format('d M Y') }}</td><td>{{ ucfirst(str_replace('_', ' ', $rfq->current_status)) }}</td></tr>@empty<tr><td colspan="3" class="text-muted text-center">No RFQs available.</td></tr>@endforelse</tbody></table></div></div></div><div class="col-lg-5"><div class="card"><div class="card-header"><h5 class="mb-0">Complaints</h5></div><div class="card-body">@forelse($customer->complaints as $complaint)<div class="border-bottom pb-2 mb-2"><strong>{{ $complaint->subject }}</strong><span class="badge bg-label-warning float-end">{{ ucfirst($complaint->status) }}</span><small class="d-block text-muted">{{ $complaint->reported_date?->format('d M Y') }}</small></div>@empty<p class="text-muted mb-0">No complaints available.</p>@endforelse</div></div></div></div>
@endsection
